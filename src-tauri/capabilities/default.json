{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "阅读条与设置窗口的核心 API 权限(事件订阅/窗口等)",
  "windows": ["reader", "settings"],
  "permissions": [
    "core:default"
  ]
}
